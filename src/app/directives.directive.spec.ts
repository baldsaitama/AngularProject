import { DirectivesDirective } from './Role.directive';

describe('DirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
